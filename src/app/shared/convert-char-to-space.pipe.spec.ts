import { ConvertCharToSpacePipe } from './convert-char-to-space.pipe';

describe('ConvertCharToSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertCharToSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
